# R-231 R-227 result audit

Date: 2026-08-02

Verdict: **GO TO REJECT S13 AS MEASURED NO-CHANGE**

## Independent decision

The read-only independent auditor found no blocking issue. S13 is rejected,
S14 is not applicable because no algorithm generation was admitted, and the
continuous panel may advance to S15.

The auditor reran no long input and edited no file.

## Verified authority and evidence

- exact implementation commit and public branch:
  `5d720128ccbd77091ac613274df135b503af028c`;
- terminal-record SHA-256:
  `75445b6fdbc90d0ae3ded651a0e239dde4bcb8a1f1c9a48c8f438374726b7db5`;
- run-index SHA-256:
  `93e77f87b84b98d46b60fbffd70b1688b117ec974142842f8ddf174be8cb9301`;
- Mozart receipt SHA-256:
  `415573e6f30bef2d339b36423ed8a3476fa1e7e12c9ff5bf42a07516ad0f7c72`;
- Mozart eligibility-file SHA-256:
  `e19da53d24f52dd8908ddda31fe2bdcd0896ee432677df8335580a7ebbb92b8f`;
- long-LibriSpeech receipt SHA-256:
  `6ab6c2178193bd03ff3b8c171e14aa5c2435bf1e4cbd685e9c4ab7c710595cea`;
- long-LibriSpeech eligibility-file SHA-256:
  `52cdf17de9f0641e9e36c62cace1194143fe969c3b4b0fb2e83ffe1f96eacea4`.

Both completed real inputs have zero lanes, zero subsets and
`phase_accessed=false`. Their internal eligibility digests also match their
receipts. Each independently fails the frozen all-three-real admission gate.

The two published inputs remained inside the 4 GiB peak-RSS, 8 GiB working-
storage and per-input wall-time ceilings. Total retained run evidence is
246,527,368 bytes, below the 12 GiB bound.

The *Elephants Dream* staging directory exists and is exactly empty. There is
no final directory or candidate. The recorded
`MemoryError: native lapped analysis exceeds the configured host ceiling`
matches the frozen 512 MiB native lapped-analysis ceiling reached through the
direct-Truth encode path. The synthetic control has neither staging nor final
output and was correctly not run after the terminal resource failure.

The run index retains `RUNNING` only because the exception occurred before
aggregate finalization. The separate exact-hash `FAIL_CLOSED` record correctly
binds that state without mutating published input evidence. No retry artifact
or active R-227 process exists. The earlier missing-`PYTHONPATH` incident began
no input and created no output root, so it is not an experiment retry.

## Scope

This GO authorizes only the negative S13 closure, the not-applicable S14 state,
and evidence-first S15 planning. It authorizes no phase syntax, resource-bound
change, decoder/default/version change, Opus rerun, compression claim,
promotion or release.
